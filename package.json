{
  "name": "chronicles-of-ethium",
  "type": "module",
  "version": "0.0.1",
  "scripts": {
    "dev": "node scripts/sync-publish.mjs && astro dev",
    "start": "npm run dev",
    "publish": "node scripts/sync-publish.mjs",
    "build": "node scripts/sync-publish.mjs && astro build",
    "preview": "astro preview"
  },
  "dependencies": {
    "astro": "^5.7.0"
  },
  "engines": {
    "node": ">=22.12.0"
  }
}
